const initialState = {
    settingsReducer: {
        settings: {
            columns: 5,
            rows: 5,
            mines: 5,
            totalFlags: 5
        }
    }
};

export default initialState
